{% set version = "3.4.2" %}
{% set sha256 = "502bc2820de6ba6c14873f2b0db34baddb152abb519d60214351cacf500b4aa9" %}

package:
  name: orange3
  version: {{ version }}

source:
  fn: Orange3-{{ version }}.tar.gz
  url: https://github.com/biolab/orange3/archive/{{ version }}.tar.gz
  sha256: {{ sha256 }}
  patches:
    - requirements.patch # conda does not yet provide scikit-learn 0.18.1

build:
  number: 0
  script:
    - python setup.py install --single-version-externally-managed --record=record.txt
  entry_points:
    - orange-canvas = Orange.canvas.__main__:main
  osx_is_app: True
  skip: True        # [py2k]

requirements:
  build:
    - toolchain
    - python
    - setuptools
    - numpy x.x
  run:
    - python
    - setuptools
    - numpy x.x
    - numpy         >=1.9.0
    - scipy
    - scikit-learn  >=0.18
    - bottleneck
    - chardet       >=2.3.0
    - docutils
    - xlrd          >=0.9.2
    - anyqt
    - pyqtgraph
    - joblib
    - keyring
    - keyrings.alt
    - dill
    - pip
    - python.app    # [osx]
   
test:
  commands:
    - python -c "import pkg_resources; pkg_resources.require('Orange3')"
    - orange-canvas --help
  imports:
    - Orange

app:
  entry: orange-canvas
  summary: 'data visualization and data analysis tool'
  icon: icon_48x48.png

about:
  home: http://orange.biolab.si/
  license: GPL3
  license_file: LICENSE
  summary: component-based data mining framework
  description: |
    Open source data visualization and data analysis for novice and expert.
    Interactive workflows with a large toolbox.
  doc_url: http://orange.biolab.si/docs/
  dev_url: https://github.com/biolab/orange3

extra:
  recipe-maintainers:
    - astaric
    - lanzagar
    - ales-erjavec
